include:
  - component: $CI_SERVER_FQDN/to-be-continuous/helm/gitlab-ci-helm@8.2.0
    inputs:
      repos: ""

helm-unittest:
  #extends: .helm-base
  stage: test
  image: helmunittest/helm-unittest
  needs:
    - job: helm-lint
      artifacts: false
  script:
    - helm unittest .
